# DESIGN-extfee19 — 自園生徒の早朝・延長料金（19時以降ルール）とワンタップ受付

マーカー: `SF-EXTFEE19-20260911`　対象: `apps/timely.html` のみ

## 料金表（自園生徒・会計時に「実お迎え時刻」で判定）※2026-09-11 現場確認済みの確定版

| 区分 | 月極あり | 月極なし（フラグ無しの園児を含む） |
|---|---|---|
| 早朝 | ¥0 | ¥700（時刻では変わらない） |
| 延長・実お迎え 19:05 まで | ¥0 | **¥0**（全員無料） |
| 延長・実お迎え 19:06 以降（20:00 閉園まで） | **¥0**（延長月極は 19:06 以降も無料） | ¥700 を**1回だけ** |

- 延長で時間以外に金額が変わる要素は無い（19:06 以降の ¥700 は1回だけ。それ以外の加算はしない）。
- 境目（19:05/19:06）は定数 `SF_EXT19_CUTOFF`。金額は既存の早朝・延長の料金定数（`USAGE_TYPES.*.flatRate`）を使い、新しい金額のハードコードはしない。
- 領収書の明細は「延長料金（19時以降）」と分かる表記にする。

## 月極の判定

- 名簿 `kindergarten_public/roster` の各園児の `monthlyAm`（早朝）／`monthlyPm`（延長）。
  月謝アプリ側は `a3b6b77`（SF-MONTHLYOPT-ROSTER-20260911）で名簿に載せる対応済み・本番反映済み。
- **roster にフラグが無い園児・名簿が読めないときは「月極なし」として扱う**
  （早朝 ¥700／延長は 19:06 以降のみ ¥700）。
- visit には判定に使った月極フラグ（`kinderMonthly`）を控える。フラグ未記載の旧 visit は
  会計時に名簿を引き直し、それでも不明なら月極なし。

## 補足（設計の確定事項）

1. **予約ボードの時刻は料金判定に一切使わない。** 判定は会計時の実お迎え時刻のみ。
2. **月極の子の ¥0 会計は会計記録を作らない**（会計待ちから外すだけ）。
   件数は sf_bookings（SF_MIRROR）から数えるので月報の件数は減らない。
3. ワンタップ受付の一覧に出すのは、**延長（ext_pm）は本日の予約全員、早朝（ext_am）は月極でない子だけ**。

## ワンタップ受付（内部生タブ）

- 内部生タブ上部に予約ボードの予約一覧を表示。
  対象 = `SF_MIRROR` のうち `affiliation==='youchisha'` ∧ `gardenId` がこの園 ∧ 日付が内部生タブで選択中の日（既定は本日）。
- 各行に氏名・区分（早朝/延長）・予約時刻・月極の有無。
- タップすると手入力と同じ関数（`sfBuildInternalVisit`）で同じ形の visit を作り会計待ちへ
  （`ext_am`→`morning` / `ext_pm`→`extension`）。
- visit に `sfId` を控え、同じ `sfId` の visit が既にあれば「受付済み」と表示してタップ不可。

## 変えないもの

- `importBoardReservations` の youchisha×ext 除外（SF-KINDER-TIMELY-HIDE-STEP4-V6-20260702）
- `sfKinderExtCounts` の件数集計（件数は共有予約から・金額は会計記録から）
- 売上合計の集計方法／判定エンジン／board 5本
